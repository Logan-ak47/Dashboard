[project]
name = "opsboard"
version = "0.1.0"
description = "An internal transaction metrics dashboard built while learning Django."
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "django>=5.2,<5.3",
]

[dependency-groups]
dev = []

